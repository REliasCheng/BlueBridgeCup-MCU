# 调试记录：问题、修正与板端检查

这里按工程记录源码问题、修正位置和下一次板端检查项。训练源码保留原状，修改集中在 [practice](../practice/peripheral-driver-corrections/README.md)。

| 编号 | 项目 | 代码问题 | 修正 | 板端检查 |
|---|---|---|---|---|
| 01 | 独立按键 | 赋值分支之外读取局部变量 | 读取和处理限制在同一扫描路径 | 空闲状态是否误触发 |
| 02 | UART/RTC | 上电写固定时间；TI职责混用 | RTC按有效性初始化；ISR统一处理TI | 连续命令与断电保持 |
| 03 | PCF8591 | 接收变量未初始化、ACK未返回、换算系数固定 | 初始化接收字节并返回ACK结果 | 原始ADC、输入电压、显示值 |
| 04 | DS18B20 | `0x44` 后立即读取 | 启动转换与读取分开，提供750 ms入口 | 上电首值与温度变化 |
| 05 | 国赛方向训练 | 页面数据使用固定初值 | 保持为状态与界面输入 | 页面切换与参数边界 |
| 06 | LED 模式训练 | I²C接收初值；P3按键/UART复用 | 使用修正版驱动并分模式检查 | 拨码、按键、UART、ADC/EEPROM |

## 01 按键 `key_value` 的有效性

- **问题**：[独立按键工程](../projects/key-demo/README.md)的 `Key_Task()` 中，局部变量在某些调用路径上可能未赋值。
- **现象**：`key_value = Read_4Key()` 位于 `Count_Key >= 1` 分支内，后面的 `if(key_value == 4/5/6/7)` 在分支外。静态检查就能找到这条路径；暂无板端误触发记录。
- **原因**：扫描时机判断与键值处理范围没有保持一致。
- **修正**：[practice实现](../practice/peripheral-driver-corrections/key_task_safe.c)初始化键值，并只在扫描到期的分支中读取和处理。
- **检查**：消抖阈值之外，还要确认每次进入 `Key_Task()` 时键值是否有效。

## 02 UART 输出与 RTC 时间语义

- **问题**：[UART 工程](../projects/uart-demo/README.md)能按 `time1` 输出字符串，但输出内容并不自动代表当前时间。
- **现象**：`System_Init()` 调用 `Set_Time(0x23, 0x59, 0x55)`；`Year` 在源码中固定为 2026。
- **原因**：当前版本先演示串口命令与 RTC 读取，没有实现“仅首次启动设时”或外部校时流程。
- **修正**：[RTC策略](../practice/peripheral-driver-corrections/rtc_startup.c)先检查CH位和BCD范围，只在值无效时写默认时间；[UART实现](../practice/peripheral-driver-corrections/uart_safe.c)由ISR统一处理TI和RI。
- **检查**：分别记录串口输出、RTC寄存器值和连续走时；连续发送命令时检查 `putchar()` 与 ISR 的 `TI` 处理顺序。

## 03 I²C 接收与 ADC 电压换算

- **问题**：[PCF8591 工程](../projects/i2c-adc-demo/README.md)要先取得有效 ADC 字节，才能讨论电压换算。
- **代码现象**：`I2CReceiveByte()` 中 `da` 未初始化就移位；`I2CSendByte()` 读取 ACK 后未用返回值处理 NACK；`main.c` 用固定 `0.0196` 系数换算。当前没有 ADC 原值、输入电压和显示值的同步记录。
- **分析**：接收字节的有效性、通信应答与电压标定是三层问题。构建成功或数码管显示数字不足以判断测量精度。
- **修正**：[I²C修正版](../practice/peripheral-driver-corrections/iic_safe.c)把接收变量初始化为0，返回地址/数据阶段ACK结果，并在单字节读取结束发送NACK。
- **检查**：先记录 I²C 应答和原始字节，再核对换算值，最后用万用表完成电压标定。

## 04 DS18B20 转换等待

- **问题**：[DS18B20 工程](../projects/ds18b20-demo/README.md)的 `Read_Temp()` 在发送 `0x44` 后紧接着重新复位并读取。
- **现象**：代码没有单独的转换完成等待或轮询路径；目前没有上电首值、室温和加热后的连续读数记录。
- **原因**：温度转换不是瞬时完成；过早读取可能得到旧值或不稳定值，实际表现还与器件配置有关。
- **修正**：[DS18B20修正版](../practice/peripheral-driver-corrections/ds18b20_safe.c)拆分启动转换和读取，并提供等待750 ms的阻塞入口。
- **检查**：记录启动转换、读取暂存器和页面刷新三个时间点，并观察温度变化过程。

## 05 综合训练工程的完成范围

- **问题**：[国赛方向模拟训练](../competition/national/16th-practice/README.md)已有数据、参数和校准界面，各需求项仍需分别核对输入与输出。
- **现象**：`temp`、`level`、`vol`、`wt` 使用固定初值；`Logic_Task()` 把蜂鸣器和继电器保持关闭。DS1302 时间读取和按键页切换则有实际代码路径。
- **原因**：当前版本主要搭建交互状态与任务框架，测量输入仍使用固定值，执行器逻辑保持关闭。
- **修正**：项目页将界面、RTC 和按键任务与固定数据分开说明。
- **检查**：按需求项分别记录输入、预期输出和板端结果。

## 06 LED 模式、EEPROM 与 UART 资源

- **问题**：[LED 模式综合训练](../competition/national/led-mode-training/README.md)把参数保存、ADC 亮度和串口报告放在同一程序中，驱动读数与端口复用会影响整机行为。
- **代码现象**：`iic.c` 的 `I2CReceiveByte()` 在第一次左移前未给 `da` 赋初值；`Read_4Key()` 使用 P3.0/P3.1，UART1 也使用这两个功能脚。该工程的 UART 是查询发送，没有接收命令流程。
- **原因**：I²C 数据有效性影响采样与参数保存，P3 端口复用影响按键与串口，两条路径需要分开定位。
- **检查**：按“拨码/按键 → 串口 → ADC/EEPROM”的顺序分开复测。

## 下一次板端调试怎么记

新记录按“板型/主频 → 输入 → 实际现象 → 代码定位 → 修改提交 → 同条件复测”填写。
