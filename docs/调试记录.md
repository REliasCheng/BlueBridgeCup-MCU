# 调试记录：代码检查与板端验证点

这里按工程和函数记录已定位的代码行为、可能影响输出的条件，以及下一次板端要观察的现象。当前条目主要依据源码检查；实际修复和复测结果将在对应提交中记录。

| 编号 | 项目 | 当前证据 | 处理状态 |
|---|---|---|---|
| 01 | 独立按键 | 赋值分支之外读取局部变量 | 已定位代码路径，未改源码 |
| 02 | UART/RTC | 上电写入固定时间与年份 | 已明确示例语义，未改源码 |
| 03 | PCF8591 | 接收变量未初始化、ACK 结果未处理、换算系数固定 | 待核查读数与电压对照 |
| 04 | DS18B20 | 转换命令后立即开始读数 | 缺温度阶跃与时序复测 |
| 05 | 国赛方向训练 | 显示页含固定初值 | 按模拟训练工程呈现 |
| 06 | LED 模式综合训练 | I²C 接收变量未初始化；按键与 UART 使用部分相同的 P3 引脚 | 已定位驱动与引脚检查点 |

## 01 按键 `key_value` 的有效性

- **问题**：[独立按键工程](../projects/key-demo/README.md)的 `Key_Task()` 中，局部变量在某些调用路径上可能未赋值。
- **现象**：`key_value = Read_4Key()` 位于 `Count_Key >= 1` 分支内，后面的 `if(key_value == 4/5/6/7)` 在分支外。静态检查就能找到这条路径；暂无板端误触发记录。
- **原因**：扫描时机判断与键值处理范围没有保持一致。
- **当前处理**：保留现有源码；用四键和无按键两类输入复测，再根据结果修改变量初始化或判断范围并记录前后输出。
- **经验**：消抖阈值正确并不保证按键任务正确，必须检查每次进入 `Key_Task()` 时键值是否有效。

## 02 UART 输出与 RTC 时间语义

- **问题**：[UART 工程](../projects/uart-demo/README.md)能按 `time1` 输出字符串，但输出内容并不自动代表当前时间。
- **现象**：`System_Init()` 调用 `Set_Time(0x23, 0x59, 0x55)`；`Year` 在源码中固定为 2026。
- **原因**：当前版本先演示串口命令与 RTC 读取，没有实现“仅首次启动设时”或外部校时流程。
- **当前处理**：将 `time1` 输出按命令—响应示例理解；检查重启前后 RTC 数据，再决定如何分开初始化与校时。
- **经验**：串口打印正确、RTC 寄存器可读、时间具有现实意义是三项不同的验收内容。另需测试连续发送命令时 `putchar()` 与串口 ISR 对 `TI` 的处理顺序。

## 03 I²C 接收与 ADC 电压换算

- **问题**：[PCF8591 工程](../projects/i2c-adc-demo/README.md)要先取得有效 ADC 字节，才能讨论电压换算。
- **代码现象**：`I2CReceiveByte()` 中 `da` 未初始化就移位；`I2CSendByte()` 读取 ACK 后未用返回值处理 NACK；`main.c` 用固定 `0.0196` 系数换算。当前没有 ADC 原值、输入电压和显示值的同步记录。
- **分析**：接收字节的有效性、通信应答与电压标定是三层问题。构建成功或数码管显示数字，都不能单独证明测量准确。
- **当前处理**：先核查字节接收和应答路径，再用万用表取多个输入点，记录 `输入 V / ADC 原值 / 显示 V`；确认后分别修改通信或换算代码。
- **经验**：先验证 I²C 收发，再验证数据换算，最后验证测量精度。

## 04 DS18B20 转换等待

- **问题**：[DS18B20 工程](../projects/ds18b20-demo/README.md)的 `Read_Temp()` 在发送 `0x44` 后紧接着重新复位并读取。
- **现象**：代码没有单独的转换完成等待或轮询路径；目前没有上电首值、室温和加热后的连续读数记录。
- **原因**：温度转换不是瞬时完成；过早读取可能得到旧值或不稳定值，实际表现还与器件配置有关。
- **当前处理**：记录温度阶跃与调用耗时；确认结果后，再调整“启动转换/等待/读取”的时序。
- **经验**：界面每 750 ms 更新一次，不等于一次 `Read_Temp()` 内部已经等够转换时间。

## 05 综合训练工程的完成范围

- **问题**：[国赛方向模拟训练](../competition/national/16th-practice/README.md)有数据、参数、校准界面，但不能只凭界面数量判断赛题完成度。
- **现象**：`temp`、`level`、`vol`、`wt` 使用固定初值；`Logic_Task()` 把蜂鸣器和继电器保持关闭。DS1302 时间读取和按键页切换则有实际代码路径。
- **原因**：当前版本主要搭建交互状态与任务框架，测量输入和执行器逻辑尚未形成有证据的闭环。
- **当前处理**：项目页将界面、RTC 和按键任务与固定数据分开说明；后续按需求项记录输入、预期输出和板端结果。
- **经验**：竞赛工程的验收单位是需求项，不是源文件数量、界面数量或 Keil 的 0 error。

## 06 LED 模式、EEPROM 与 UART 资源

- **问题**：[LED 模式综合训练](../competition/national/led-mode-training/README.md)把参数保存、ADC 亮度和串口报告放在同一程序中，驱动读数与端口复用会影响整机行为。
- **代码现象**：`iic.c` 的 `I2CReceiveByte()` 在第一次左移前未给 `da` 赋初值；`Read_4Key()` 使用 P3.0/P3.1，UART1 也使用这两个功能脚。该工程的 UART 是查询发送，没有接收命令流程。
- **定位**：先在读取路径确认 ADC 原值和 EEPROM 重启后的参数，再按板上键盘/串口切换方式单独检查按键与串口报告，不能把两类现象归因于同一个任务函数。
- **处理记录**：保留训练时的源码，在项目页明确发送方式与板端复测顺序；没有将这次代码检查写成已完成的硬件修复。

## 下一次板端调试怎么记

新记录按“板型/主频 → 输入 → 实际现象 → 代码定位 → 修改提交 → 同条件复测”填写。代码检查与实物结果分别标注。
