# BlueBridgeCup-MCU

## 项目简介

这是我的蓝桥杯单片机竞赛训练与嵌入式学习记录。从 CT107D 上的 LED、数码管和按键练起，再把 UART、I²C、温度传感器和多任务界面放进独立 Keil 工程。代码留下当时的写法；文档解释电路、程序流程和发现的问题。

仓库当前有 **7 个可打开的 C51 工程**，不是教材或历届答案合集。原工程留有 0 error / 0 warning 的历史构建记录；本次文档升级没有重新烧录板卡，项目文档会明确区分代码行为、静态检查和实测结果。

## 平台环境

| 项目 | 当前仓库使用情况 |
|---|---|
| 开发板 | CT107D（资料覆盖 V31、V40） |
| 板载 MCU | IAP15F2K61S2；Keil 工程使用 `STC15F2K60S2 Series` 兼容目标 |
| 语言与 IDE | C、Keil C51 / µVision |
| 下载与调试 | STC-ISP、串口调试工具；多个练习工程按 12 MHz 配置 |

V40 下载 IAP15 时，J9、J14 的设置要以板上丝印和硬件说明为准。具体外设引脚见 [CT107D 硬件分析](docs/CT107D硬件分析.md)。

## 技术方向

- **基础控制**：GPIO、P0/P2 数据与锁存通道、LED、继电器。
- **人机交互**：数码管动态扫描、独立按键读取与消抖；训练工程进一步使用矩阵按键和多界面状态。
- **时间基础**：Timer2 中断提供扫描与任务计数节拍，Timer1 用于 UART 波特率。
- **通信与外设**：UART 命令处理、软件 I²C / PCF8591 ADC、DS18B20 1-Wire、DS1302 RTC。
- **综合设计**：把输入、显示、外设数据和业务状态安排在同一个比赛训练工程中。

这些方向都有下方的代码入口；AT24C02、PWM、超声波目前尚未作为本仓库独立项目发布。

## 学习路线

```text
基础控制（P0/P2、LED）
  ↓
显示（段码、位选、Timer2 扫描）
  ↓
输入（独立按键、消抖）
  ↓
通信（UART 中断收发与命令）
  ↓
外设（I²C/ADC、DS18B20、RTC）
  ↓
比赛综合训练（界面、任务、资源分配）
```

前六个练习是单点能力，第七个训练工程把它们放到更接近赛题的程序结构里。[学习路线](docs/学习路线.md)记录每一步对应的源码和下一次实测要补什么。

## 项目列表

| 项目 | 已写入的功能 | 可以从代码中看到什么 |
|---|---|---|
| [LED 与锁存器](projects/led-demo/README.md) | LED 位置变化、继电器定时翻转 | P0 数据、P2 选通、Timer2 节拍与状态更新 |
| [数码管动态扫描](projects/segment-display-demo/README.md) | 8 位扫描显示固定数值 | 段码表、位选、消影顺序、整数拆位 |
| [独立按键](projects/key-demo/README.md) | 按键映射为 LED 状态 | P3 输入、按下确认、事件与输出的分离 |
| [UART 命令](projects/uart-demo/README.md) | `time1` / `temp2` 命令与数据输出 | Timer1 波特率、串口中断、接收缓冲与任务处理 |
| [PCF8591 ADC](projects/i2c-adc-demo/README.md) | 采样模拟量并显示电压 | P2.0/P2.1 软件 I²C、转换与显示 |
| [DS18B20 温度](projects/ds18b20-demo/README.md) | 单总线读取并显示温度 | P1.4 时序、温度数据处理、周期采样 |
| [国赛方向模拟训练](competition/national/16th-practice/README.md) | 数据/参数/校准界面与 RTC 读取 | Timer2 统一节拍、矩阵键、状态与多任务组织 |

训练工程是阶段练习，不是正式参赛作品或完整赛题答案。项目页面分别写明目前的代码范围和调试记录。

## 硬件与软件的对应

```text
IAP15 / STC15 兼容工程
  ├─ P0 输出 8 位数据
  └─ P2.5～P2.7 → 74HC138 译码 → 74HC573 锁存
                                       ├─ LED
                                       ├─ 蜂鸣器 / 继电器
                                       ├─ 数码管位选
                                       └─ 数码管段选
```

`Control(0x80, value)`、`Control(0xC0, value)` 这样的调用背后是先写 P0，再用 P2 选锁存通道。数码管在中断里按“关位 → 写段 → 开位”刷新；只看 `main()` 的循环无法理解显示为什么稳定。[CT107D 硬件分析](docs/CT107D硬件分析.md)把各项目中的端口、函数和外设对应起来。

## 编译与运行

1. 进入某个项目目录，先读它的 README，再用 Keil C51 打开同目录的 `.uvproj`。
2. 核对兼容目标器件、12 MHz 配置、具体板型与拨码/跳线。不同工程共用 P3、P2 引脚时尤其要检查连接。
3. Build 后用 STC-ISP 下载生成的 HEX，按项目 README 的输入和预期输出逐项观察。
4. 把实际现象、照片与修改提交补进 [调试记录](docs/调试记录.md)。仅编译通过不等于烧录和硬件功能通过。

仓库保留每个练习原有的 C 源码与 Keil 相对路径；部分中文源码注释原为 GBK 编码，`.gitattributes` 让 GitHub 页面可读而不改本地原文件字节。构建输出、PDF、安装器和整套参考答案都不入仓库。

## 调试与竞赛训练

[调试记录](docs/调试记录.md)写明按键变量赋值、RTC 启动设时、ADC 换算和单总线转换等待等具体代码行为；发现与修复分开记录，不把未做过的板端测试写成结果。[蓝桥杯竞赛体系](docs/蓝桥杯竞赛体系.md)说明单模块练习如何过渡到需求拆分、资源分配和集成测试。

## 后续计划

先给现有工程补板端照片、串口输出和测量记录，再增加 AT24C02、PWM、超声波和经过实测的省赛综合项目。多个项目需要共用驱动时再抽公共层；目前保持独立工程，避免搬动源码破坏 Keil 路径。

STM32、FreeRTOS、Linux 和 ROS 属于之后的学习方向；本仓库不把计划写成现有项目，也不伪造竞赛成绩或实验历史。
